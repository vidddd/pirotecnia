{% extends 'JordiLlonchCrudGeneratorBundle::layout.html.twig' %}

{% block title %}
{{ parent() }} - {{ 'views.show.show'|trans({'%entity%': 'Libro'}, 'JordiLlonchCrudGeneratorBundle') }}
{% endblock %}

{% block page %}

<h1>Libro</h1>

<form class="form-horizontal">
    <fieldset>        <div class="control-group">
            <label class="control-label">Id</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.id }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Fecha</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.fecha|date('Y-m-d H:i:s') }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Cartaporte</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.cartaporte }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Clase</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.clase }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Procedenciad</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.procedenciad }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Folio</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.folio }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Entrada</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.entrada }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Salida</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.salida }}</span>            </div>
        </div>        <div class="control-group">
            <label class="control-label">Existencias</label>
            <div class="controls">                <span class="input-xlarge uneditable-input">{{ entity.existencias }}</span>            </div>
        </div>    </fieldset>
</form>

<div>
    <div class="float-left">
        <a class="btn" href="{{ path('libro') }}">
            {{ 'views.recordactions.backtothelist'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}
        </a>
    </div>
    <div class="float-left">
        <a class="btn btn-primary" href="{{ path('libro_edit', { 'id': entity.id }) }}">
            {{ 'views.recordactions.edit'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}
        </a>
    </div>
    <div class="float-left">
        <form action="{{ path('libro_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button class="btn btn-danger" type="submit" onclick="return confirm('{{ 'views.recordactions.confirm'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}');">{{ 'views.recordactions.delete'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}</button>
        </form>
    </div>
</div>
{% endblock %}